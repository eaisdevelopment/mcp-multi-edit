---
phase: 11-publishing
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
user_setup:
  - service: npm
    why: "Publishing scoped package to npm public registry"
    env_vars: []
    dashboard_config:
      - task: "Create @essentialai npm organization (if not exists)"
        location: "https://www.npmjs.com/org/create"
      - task: "Log in to npm CLI: npm login"
        location: "Terminal"
  - service: github
    why: "Hosting source code repository"
    dashboard_config:
      - task: "Create eaisdevelopment/mcp-multi-edit repository (if not exists)"
        location: "https://github.com/new or gh repo create"

must_haves:
  truths:
    - "Git remote origin points to https://github.com/eaisdevelopment/mcp-multi-edit"
    - "Git author is configured as Pavlo Sidelov <pavlo@essentialai.uk>"
    - "Code is pushed to GitHub repository"
    - "Package is published to npm as @essentialai/mcp-multi-edit@0.1.0"
  artifacts: []
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "npm publish --access public"
      pattern: "@essentialai/mcp-multi-edit"
    - from: "git remote"
      to: "github.com"
      via: "git push -u origin main"
      pattern: "eaisdevelopment/mcp-multi-edit"
---

<objective>
Configure git remote, push to GitHub, and publish to npm registry.

Purpose: Make the package available to the public via npm and ensure source code is hosted on GitHub.
Output: Code on GitHub, package on npm
</objective>

<execution_context>
@/Users/dev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-publishing/11-CONTEXT.md
@.planning/phases/11-publishing/11-RESEARCH.md
@.planning/phases/11-publishing/11-01-SUMMARY.md
@.planning/phases/11-publishing/11-02-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure git for publishing</name>
  <files></files>
  <action>
Configure the local git repository for pushing to GitHub:

1. Set local git author (repo-level, not global):
   ```
   git config user.name "Pavlo Sidelov"
   git config user.email "pavlo@essentialai.uk"
   ```

2. Add the GitHub remote (if not already set):
   ```
   git remote add origin https://github.com/eaisdevelopment/mcp-multi-edit.git
   ```
   If origin already exists, update it:
   ```
   git remote set-url origin https://github.com/eaisdevelopment/mcp-multi-edit.git
   ```

3. Stage and commit all phase 11 changes with the configured author:
   ```
   git add package.json src/server.ts LICENSE CHANGELOG.md README.md
   git commit -m "chore(11): prepare package for publishing as @essentialai/mcp-multi-edit v0.1.0"
   ```

4. Verify configuration:
   - `git config user.name` shows "Pavlo Sidelov"
   - `git config user.email` shows "pavlo@essentialai.uk"
   - `git remote -v` shows eaisdevelopment/mcp-multi-edit
   - `git log -1 --format="%an <%ae>"` shows "Pavlo Sidelov <pavlo@essentialai.uk>"
  </action>
  <verify>
`git config user.name` returns "Pavlo Sidelov". `git config user.email` returns "pavlo@essentialai.uk". `git remote -v` contains "eaisdevelopment/mcp-multi-edit". `git log -1` shows the phase 11 commit with correct author.
  </verify>
  <done>Git configured with correct author identity, remote pointing to eaisdevelopment/mcp-multi-edit, all phase 11 changes committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push to GitHub and publish to npm</name>
  <files></files>
  <action>
CHECKPOINT: Human verification required for GitHub push and npm publish.

All package files are prepared and committed locally:
- package.json updated with @essentialai/mcp-multi-edit identity
- LICENSE replaced with PolyForm Noncommercial 1.0.0
- CHANGELOG.md created with 0.1.0 entry
- README.md rewritten for Claude Code audience
- src/server.ts version updated to 0.1.0
- Git configured with correct author and remote

The user must perform the following steps:

**Prerequisites (do these first if not done already):**

1. Ensure the GitHub repository exists:
   ```bash
   gh repo create eaisdevelopment/mcp-multi-edit --public --source=. --push
   ```
   OR create manually at https://github.com/new

2. Ensure the npm organization exists:
   - Visit https://www.npmjs.com/org/create
   - Create organization named "essentialai" (free for public packages)

3. Ensure you are logged into npm:
   ```bash
   npm login
   npm whoami  # should show your npm username
   ```

**Push to GitHub:**
```bash
git push -u origin main
```

**Publish to npm:**
```bash
npm publish --access public
```
  </action>
  <verify>
User verifies:
1. Visit https://github.com/eaisdevelopment/mcp-multi-edit -- should show repository with all code
2. Visit https://www.npmjs.com/package/@essentialai/mcp-multi-edit -- should show package listing
3. Test npx installation in a temp directory:
   ```bash
   mkdir /tmp/test-mcp && cd /tmp/test-mcp
   npx @essentialai/mcp-multi-edit
   # Should start the MCP server (ctrl+c to stop)
   ```

Type "published" when package is live on npm and code is on GitHub, or describe any issues encountered.
  </verify>
  <done>Source code is on GitHub at eaisdevelopment/mcp-multi-edit. Package is published on npm as @essentialai/mcp-multi-edit@0.1.0. npx @essentialai/mcp-multi-edit starts the MCP server.</done>
</task>

</tasks>

<verification>
1. Git remote configured to eaisdevelopment/mcp-multi-edit
2. Git author is Pavlo Sidelov <pavlo@essentialai.uk>
3. Code pushed to GitHub (user verifies)
4. Package published to npm (user verifies)
5. npx @essentialai/mcp-multi-edit starts the server (user verifies)
</verification>

<success_criteria>
- Source code is publicly available on GitHub at eaisdevelopment/mcp-multi-edit
- Package is published on npm as @essentialai/mcp-multi-edit@0.1.0
- npx @essentialai/mcp-multi-edit starts the MCP server successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-publishing/11-03-SUMMARY.md`
</output>
